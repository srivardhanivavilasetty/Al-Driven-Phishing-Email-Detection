# Models

This folder stores trained artifacts for the phishing detection application.

- `best_model.pkl` - Saved best machine learning model
- `tfidf_vectorizer.pkl` - Saved TF-IDF vectorizer
- `model_results.json` - Model comparison results
- `feature_config.json` - Metadata and text feature configuration
